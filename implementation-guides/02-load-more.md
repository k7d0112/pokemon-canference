# 実装課題2: 追加読み込み実装

## Claude Codeへの実装プロンプト

```
ポケモン図鑑アプリに追加読み込み機能を実装してください。
実装課題1が完了した状態に、「もっと見る」ボタンによる追加読み込み機能を追加します。

## 前提条件
- 実装課題1が完了していること（ポケモンリストが24件表示され、選択機能が動作）
- 現在は24件のみ表示され、それ以上読み込むことができない状態
- この課題を完了すると、151匹まで段階的に読み込めるようになる

## 実装場所
- ファイル: src/app/page.tsx
- 実装箇所（2つ）:
  1. loadMorePokemon関数（追加読み込み処理）
  2. 「もっと見る」ボタンUI

## 機能要件
1. 「もっと見る」ボタンで追加24件を既存リストに追加表示
2. 最大151匹（第1世代）まで読み込み可能
3. ローディング中はボタンが無効化され、スピナー表示
4. 151匹に達したらボタンが非表示になる

## 完了後の動作
- ✅ 「もっと見る」ボタンが表示される
- ✅ ボタンクリックで追加24件が読み込まれる
- ✅ ローディング中はボタンが無効化される
- ✅ 151匹に達するとボタンが消える

## 実装仕様

### Part 1: loadMorePokemon関数（追加読み込み処理）

#### 処理要件
「もっと見る」ボタンがクリックされた時に呼ばれる関数で、次の24件を追加取得します。

#### 実装手順
1. **前提条件の確認**
   - if (!pokemonList) return; で早期リターン
   - 初期データがない場合は処理を行わない

2. **追加データの取得処理**
   - try-catch-finally構文でエラーハンドリング
   - tryブロック内で以下を実装:
     - setLoading(true)でローディング状態開始
     - const currentCount = pokemonList.results.length で現在の件数を取得
     - pokemonService.getPokemonList(24, currentCount)で追加データを取得

3. **データマージ処理**
   - setPokemonListで既存データと新規データを結合
   - 関数型更新パターンを使用: setPokemonList(prevList => ...)
   - 結合オブジェクトの構造:
     ```javascript
     {
       ...newData, // APIレスポンスのメタ情報を更新
       results: [...(prevList?.results || []), ...newData.results] // 既存配列と新規配列を結合
     }
     ```

4. **エラー処理と後処理**
   - catchブロック: コンソールエラー出力
   - finallyブロック: setLoading(false)

### Part 2: 「もっと見る」ボタンUI

#### 表示要件
以下の条件を満たす場合のみボタンを表示:
- pokemonListが存在する（データが読み込まれている）
- pokemonList.results.lengthが151未満（まだ追加読み込み可能）

#### UI実装仕様

**全体の構造:**
- 条件付きレンダリングで表示制御
- 中央寄せのレイアウト
- 適切な余白設定

**ボタンの実装:**
- onClick={loadMorePokemon}でクリック時の処理を設定
- disabled={loading}でローディング中は無効化
- 適切なスタイリング（グレー系）

**ボタンの表示内容:**
- ローディング中: Loader2アイコン + "読み込み中..."
- 通常時: "もっと見る"

**重要なポイント:**
- 条件分岐でローディング状態に応じた表示を切り替え
- 無効化状態でユーザーの誤操作を防ぐ

## 使用する要素の説明
- **pokemonService.getPokemonList(limit, offset)**: APIからポケモンリストを取得
- **setLoading/loading**: ローディング状態を管理するstate
- **setPokemonList/pokemonList**: ポケモンリストデータを管理するstate
- **Button**: shadcn/uiのボタンコンポーネント
- **Loader2**: lucide-reactのローディングアイコン

## 実装時の注意点
- スプレッド演算子（...）を使った配列とオブジェクトの適切な結合
- 関数型更新でprevListを使用した安全なstate更新
- 条件付きレンダリング（&&演算子）の適切な使用

## 動作確認手順
この実装が完了したら、以下を確認してください：

1. **ボタン表示の確認**
   - 「もっと見る」ボタンが表示されることを確認
   - 151件未満の場合のみ表示されることを確認

2. **追加読み込みの確認**
   - ボタンをクリックして追加24件が表示されることを確認
   - 既存のリストに追加されて、合計件数が増えることを確認

3. **ローディング状態の確認**
   - ローディング中はボタンが無効化されることを確認
   - スピナーと「読み込み中...」が表示されることを確認

4. **上限の確認**
   - 151件に達したらボタンが消えることを確認

5. **エラーがないことの確認**
   - 開発者ツールのコンソールにエラーが出ていないことを確認

完了後、段階的にポケモンを読み込める実用的なポケモン図鑑として動作します！
```